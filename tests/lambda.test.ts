import { updateReportData } from '../src/tasks/ReportTask';

desribe('ReportTask lambda functions', () => {
  test('testing updateReportData()', () => {
    /*
     * run updateReportData() with a testing dynamodb table to check
     * the format of the data created and the number of rows created
     */
  });

  test('testing function_name()', () => {
    /*
     * write additional tests here
     */
  });
});
